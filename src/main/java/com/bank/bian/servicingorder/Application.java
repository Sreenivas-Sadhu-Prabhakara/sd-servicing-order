package com.bank.bian.servicingorder;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Servicing Order
 *
 * Business Area:      Sales and Service
 * Business Domain:    Channels
 * Functional Pattern: Process
 * Asset Type:         Servicing Order
 * Control Record:     Servicing Order Procedure
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Servicing Order — BIAN Semantic API")
                .description("Service Domain 'Servicing Order' (Process pattern) "
                        + "operating on the 'Servicing Order Procedure' control record.")
                .version("0.1.0"));
    }
}
